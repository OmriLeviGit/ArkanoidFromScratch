import biuoop.DrawSurface;
import game.shapes.circles.Point;

class Ass3GameTest {

//
//    /* prints the coordinates of the ball each time it collides with something */
//
//    // goes at the beginning of move one step
//    Point startingPoint = this.center;          // TODO remove
//    double dx = this.getVelocity().getDx();     // TODO remove
//    double dy = this.getVelocity().getDy();     // TODO remove
//    // goes at the end of move one step
//    // TODO print ball coordinates
//    double startX = startingPoint.getX();
//    double startY = startingPoint.getY();
//    double endX = this.center.getX();
//    double endY = this.center.getY();
//    System.out.println("the ball is moving from (x: " + startX + ", " + startY + ") to "
//            + "(y: " + endX + ", " + endY + ")");
//    System.out.println("at velocity: (dx: " + dx + ", dy: " + dy + ")");
//
//
//
//    /* run function with controlled reps and half fps */
//    public void run(int reps) { // TODO DELETE, used for internal testing, here and other places
//        int framesPerSecond = 60;
//        int millisecondsPerFrame = 1000 / framesPerSecond;
//        for (int i = 0; i < reps; i++) {
//            long startTime = System.currentTimeMillis(); // timing
//
//            DrawSurface d = gui.getDrawSurface();
//            this.sprites.drawAllOn(d);
//            gui.show(d);
//            this.sprites.notifyAllTimePassed();
//
//            // timing
//            long usedTime = System.currentTimeMillis() - startTime;
//            long milliSecondLeftToSleep = 2 * millisecondsPerFrame - usedTime;
//            if (milliSecondLeftToSleep > 0) {
//                sleeper.sleepFor(milliSecondLeftToSleep);
//            }
//        }
//    }

}